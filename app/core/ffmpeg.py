# Este módulo es un esqueleto reservado para futuras refactorizaciones.
# La lógica de construcción del comando FFmpeg y RecordingManager vive actualmente en main.py.
# Si se decide separar por capas, mover aquí: RecordingManager, _gpu_sync, _get_processes_sync.
